{
  "name": "uni-uta-db",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node scripts/check_project.mjs",
    "test": "vitest run",
    "verify": "npm run check && npm test"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
